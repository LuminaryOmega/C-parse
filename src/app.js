// Constellation Parser Suite — App Init

console.log("%c✨ Constellation Parser Loaded", "color:#f0a; font-weight:bold;");

// Boot sequence placeholders
window.Constellation = {
    file: null,
    parsed: null,
    filters: {},
};

// UI bootstrap
window.addEventListener("DOMContentLoaded", () => {
    console.log("%c✅ UI Ready", "color:#0f0;");
});
